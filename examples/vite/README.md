# Vite example

Run `pnpm install && pnpm exec vite examples/vite`. Add `web-sdk-pp-ocrv6@0.1.5` to your application and let Vite emit the SDK Worker and ONNX Runtime Web resources.

This minimal entry uses CPU WASM and Worker execution. Change `backend` to `webgpu` only after checking browser capability.
