# Vanilla example

Run from the repository root: `pnpm install && pnpm exec vite examples/vanilla`. This is the framework-neutral portability baseline using `web-sdk-pp-ocrv6@0.1.2`.

Choose an image and press OCR. The example uses small detection/recognition models, CPU WASM, Worker execution, and explicit no-fallback behavior.
