# 公开 npm 0.2.0 示例验收

核验日期：2026-09-08（Asia/Shanghai）。分支基于发布提交 `ccecf95adcd18517c802828b45577c3c5985623c`，属于单 SDK 示例层。

Vanilla、React、Vite、CDN、微信 web-view 五种示例固定使用公开 `web-sdk-pp-ocrv6@0.2.0`。同步 README、锁文件及既有测试断言，并将 0.1.8 的旧限制说明替换为当前示例行为：主线程执行、使用 ORT 资源地址、取消后由任务 finally 释放会话。SDK 本身的初始化释放及 `RuntimeOptions.wasmPaths` 能力链接到当前 API。pnpm 仅对刚发布的自有精确版本加入 `minimumReleaseAgeExclude`，其余依赖解析不变。

实际 registry 包 SHA-512 为 `sha512-UUzUyHXNg1yGpbgw/DjF3O0bQaRQG0pcDWoOyCM1C6W5q/C+MJYTuIPHkMyedTYvRfIdvJmu/EsN3LypLmcO8g==`。下载后摘要一致；公开 provenance 和 `gitHead` 指向上述发布提交。source map 中 29 个 SDK 源文件与发布提交一致，仅归一化换行。来源核验不包含 Sigstore 签名验签。

## 验证

- 修改前后标准检查 required 18 通过、0 失败、4 远程跳过，见 [before](0.2.0-examples-standard-before.json)、[after](0.2.0-examples-standard-after.json)。
- `pnpm verify` 通过，包括 119 项 SDK 单测、14 项 Demo/示例生命周期测试、文档/模型/示例契约和 SDK 类型检查。
- `node examples/tests/consumers.test.mjs` 不传 tarball：三个独立目录实际从 npm 安装 0.2.0，类型检查、构建与输出 WASM 资源检查通过。目录为本地 `C:/Users/chenm/AppData/Local/Temp/ocrv6-consumers-Q7cTtM`。
- `node examples/tests/browser.test.mjs <上述目录>`：三工程 × 两种模型来源共 6 组 WASM OCR 通过。
- 同一命令加 `--cdn`：CDN、微信 web-view × 两种来源共 4 组通过，SDK 和 ORT 从真实版本 CDN 获取。
- 原始日志仅在本地 `test-results/examples-020-*.log` 留存，不提交。SDK runtime 和模型资产没有变更。

浏览器为 Windows 10.0.26200、Intel Core i5-10400F、Chromium 151.0.7922.34、ONNX Runtime Web 1.27.0、WASM。模型传输使用现有微型 ONNX fixture；页面、SDK 与 ORT 真实执行。结果不证明官方模型源网络和完整 OCR 精度，也不新增 GPU、移动端或微信宿主兼容结论。
