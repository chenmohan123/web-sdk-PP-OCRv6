# 0.1.0 local release readiness

Verified 2026-08-24 on the local Windows workspace.

- `pnpm verify`: passed; 44 SDK tests plus repository/model/docs/example checks.
- Demo: 3 Playwright tests passed, including Chinese initial state, three-column layout, polygon/OCR linking, and 390px no-overflow.
- SDK: TypeScript check, ESM/Worker build, and runtime manifest/dictionary copy passed.
- Models: all six Git LFS ONNX assets verified for bytes, SHA-256, graph metadata, opset, and parameter counts.
- npm dry-run: 10 package files; includes `dist`, types, Worker, manifest, and dictionaries; contains no `.onnx`.
- Governance: offline portal checker reports `locally-compliant`, zero required failures and zero recommended failures. GitHub Ruleset, Pages, deployment, npm publication, and release-tag evidence remain remote `skip` until authenticated read-only evidence is collected.

The release uses Apache-2.0, official PaddlePaddle PP-OCRv6 assets, default `small-det + small-rec`, CPU WASM by default, optional WebGPU, and explicitly does not support the native WeChat mini-program runtime.
